# M1/M2 首批开发 Backlog

> 本文是创建 GitHub Issue 前的可审阅清单。编号保持执行手册顺序；发布到 GitHub 后用实际 Issue URL 回填。所有任务必须遵守 `AGENTS.md`、公共契约和 Definition of Ready/Done。

## 依赖图

```text
#1 -> #2 -> #5 -> #6 ---------> #8
 |     |      |     |             |
 |     |      |     +-----------> #7 -> #10
 |     |      +-----------------> #9
 |     +-------------------------+
 +-> #3 -> #4
      |
      +-> #11 -> #12 -> #7/#9/#10
```

#4 可在 #1 和 #3 后并行；#5 依赖 #2/#3；#11 依赖 #2/#3；#12 依赖 #2/#3/#11；#7/#9 依赖 #5/#6/#11/#12；#8 依赖 #5/#6/#11；#10 依赖 #7/#11/#12。图中的依赖只表示最早可开始条件，不授权跨 Issue 修改。

## M1 工程底座

### #1 初始化 FastAPI 模块化单体工程

- **负责人/审查者**：A / B
- **关联需求**：非功能-可维护性、`docs/architecture.md`
- **依赖**：无
- **允许修改**：`app/main.py`、`app/bootstrap.py`、`app/api/`、最小 `app/shared/`、`tests/`、`pyproject.toml`
- **禁止修改**：业务模块模型、数据库迁移、Agent 空壳、前端
- **契约影响**：实现 `GET /health`；创建应用工厂，`main.py` 只暴露应用
- **数据库影响**：无
- **验收**：应用可导入/启动；`/health` 返回 200、服务名、版本和 UTC 时间；不存在业务空壳和重型框架
- **测试**：应用创建、健康检查、未知路径；`ruff check .`、`ruff format --check .`、`pytest -q`
- **完成定义**：README 启动命令可执行，公共入口有类型标注，B 审查架构边界

### #2 配置 MySQL、SQLAlchemy 异步会话和 Alembic

- **负责人/审查者**：A / B
- **关联需求**：一致性、可恢复性、BR-010
- **依赖**：#1
- **允许修改**：`app/infrastructure/database/`、`app/config.py`、`migrations/`、`.env.example`、数据库测试配置
- **禁止修改**：业务表、生产凭据、业务 Service
- **契约影响**：提供 Engine/Session 工厂和事务依赖；实现就绪检查的数据库探测能力
- **数据库影响**：Alembic 基线，仅基础迁移环境，不提前建业务表
- **验收**：异步 Session 生命周期正确；配置从环境读取；空库 `alembic upgrade head` 成功；`alembic heads` 只有一个 head
- **测试**：配置缺失/非法 URL；连接与回滚；空库升级；Session 不跨请求复用
- **完成定义**：`.env` 被忽略、示例无真实密钥，迁移链和 downgrade 经人工检查

### #3 统一配置、日志、异常和 API 响应

- **负责人/审查者**：A / B
- **关联需求**：AI-008、AI-009、安全与可观测性
- **依赖**：#1
- **允许修改**：`app/config.py`、`app/shared/`、`app/infrastructure/logging/`、API 中间件和相关测试
- **禁止修改**：业务模块、外部平台实现
- **契约影响**：实现统一成功/错误响应、稳定错误码、`X-Request-ID`
- **数据库影响**：无
- **验收**：验证错误为 422；未处理异常为脱敏 500；请求 ID 贯穿日志/响应；密钥字段自动脱敏
- **测试**：配置优先级、验证错误、领域异常映射、未知异常、请求 ID 传入/生成、日志脱敏
- **完成定义**：`docs/api-contracts.md` 的通用响应有契约测试，日志不含连接密码和 Token

### #4 配置 Pytest、Ruff 和 GitHub Actions CI

- **负责人/审查者**：A / B
- **关联需求**：可维护性、执行手册质量门禁
- **依赖**：#1、#3
- **允许修改**：`pyproject.toml`、`tests/` 基础 fixture、`.github/workflows/`、开发说明
- **禁止修改**：生产业务逻辑、降低已有校验
- **契约影响**：无
- **数据库影响**：CI 为后续迁移测试预留 MySQL 8.0 Service
- **验收**：CI 安装锁定范围依赖并依次执行 Ruff lint、format check、Pytest；失败阻断
- **测试**：本地运行与 CI 命令一致；测试隔离环境变量；无网络依赖的单元测试稳定
- **完成定义**：工作流权限最小化、缓存键合理、不打印密钥；README 列出同一组命令

## M2 主数据、名单、权限与审计

### #5 设计产品、分类、品牌和规格模型

- **负责人/审查者**：B / A
- **关联需求**：FR-REQ-002、FR-SUB-001、FR-WL-001、FR-REC-002..003
- **依赖**：#2、#3
- **允许修改**：`app/modules/product/`、对应迁移/测试、相关契约文档
- **禁止修改**：供应商、名单、推荐排序、审批
- **契约影响**：产品查询/创建/更新 Schema；生命周期枚举；ProductQueryService
- **数据库影响**：`md_product_category`、`md_product`；业务唯一键、状态、版本和查询索引
- **验收**：分类树与品类必填字段 Schema 可保存；品牌+型号规范化去重；生命周期变化可并发控制
- **测试**：唯一约束、未知生命周期、规格 JSON 校验、分页/稳定排序、数据范围、版本冲突、迁移升级/回退
- **完成定义**：API/数据库文档同步，查询有 `EXPLAIN` 或索引依据

### #6 设计供应商和产品—供应商关系模型

- **负责人/审查者**：B / A
- **关联需求**：FR-REC-002..003、FR-PO-002、FR-DEL-004、FR-WL-001
- **依赖**：#5
- **允许修改**：`app/modules/supplier/`、产品模块公开关系契约、迁移/测试
- **禁止修改**：采购订单、供应商门户、黑名单生效
- **契约影响**：供应商 CRUD/query、产品供应商关系写入与有效期查询
- **数据库影响**：`md_supplier`、`md_product_supplier`；供应商编码/名称唯一，有效关系唯一
- **验收**：供应商状态和关系有效期可查询；失效供应商不作为默认候选；不存真实联系人测试数据
- **测试**：重复编码/名称、关系重叠、失效边界、分页、权限、乐观锁和迁移
- **完成定义**：ProductQueryService 只通过公开 Schema 返回供应商摘要

### #7 实现白名单模型、版本和有效期

- **负责人/审查者**：B / A
- **关联需求**：FR-WL-001..004、FR-REC-001..003、BR-002
- **依赖**：#5、#6、#11、#12
- **允许修改**：`app/modules/whitelist/`、对应迁移/测试、名单契约
- **禁止修改**：黑名单、推荐自然语言、审批
- **契约影响**：WhitelistService `find_effective`；白名单查询/创建/更新接口
- **数据库影响**：`wl_product_whitelist`、`wl_whitelist_history`；对象/范围/有效期索引
- **验收**：默认仅返回 at 时刻有效、数据范围内记录；变更追加版本；管理员才可写；黑名单优先原则以契约注明
- **测试**：生效/到期临界、永久有效、楼宇/品类范围、失效显式查询权限、版本冲突、审计、迁移
- **完成定义**：历史版本可还原，查询无 N+1，所有写入经 AuditService

### #8 实现历史采购记录和价格明细模型

- **负责人/审查者**：B / A
- **关联需求**：FR-REC-002..004/007..008、FR-DEL-004、BR-008
- **依赖**：#5、#6、#11
- **允许修改**：新增历史采购模块、迁移/测试、历史查询契约
- **禁止修改**：正式订单、推荐评分、ERP 在线集成
- **契约影响**：PurchaseHistoryService `search_similar`；历史分页查询接口
- **数据库影响**：`hp_purchase_record`、`hp_price_record`；源系统幂等键和历史查询索引
- **验收**：价格携带币种、税费、数量和日期；无数据明确返回空；重复来源记录不重复导入
- **测试**：Decimal 精度、不同币种不可直接聚合、日期边界、数据范围、幂等冲突、分页排序、迁移
- **完成定义**：不把价格塞入产品主表，查询结果能作为未来推荐快照证据

### #9 实现黑名单对象、复核和有效期模型

- **负责人/审查者**：B / A
- **关联需求**：FR-BL-001..008、FR-WL-004、BR-002/006/010、AC-009..010
- **依赖**：#5、#6、#11、#12
- **允许修改**：`app/modules/blacklist/`、对应迁移/测试、名单契约
- **禁止修改**：Agent 自然语言抽取、例外采购、推荐评分
- **契约影响**：BlacklistService `find_effective`；草稿、提交复核、复核和解除接口
- **数据库影响**：黑名单、复核、历史三表；对象/范围/状态/有效期索引
- **验收**：创建只能为 DRAFT；发起人不能复核；只有复核转 ACTIVE；到期转 EXPIRED；管理员解除必须说明理由；MVP 无例外接口
- **测试**：产品/品牌/供应商/组合命中、时间边界、自审阻断、越权解除、重复动作幂等、非法状态、审计、迁移
- **完成定义**：有效黑名单查询可被推荐模块独立调用，历史完整不可覆盖

### #10 实现 Excel 白名单导入与数据校验

- **负责人/审查者**：B / A
- **关联需求**：FR-WL-001..003、数据质量、重复导入和附件安全要求
- **依赖**：#7、#11、#12
- **允许修改**：白名单导入服务、受控上传接口、任务表用法、模板和测试 fixture
- **禁止修改**：通用文件存储平台、真实采购数据、直接绕过 Service 批量写库
- **契约影响**：创建导入任务、查询报告、确认提交接口；定义模板列和错误报告 Schema
- **数据库影响**：复用 `task_system_task` 和幂等记录；正式写入复用白名单 Service
- **验收**：上传先校验不落正式表；报告总数/有效/错误/重复；确认后事务化/幂等提交合法行；行错误可定位
- **测试**：正确模板、缺列、非法日期/对象、重复行、大文件限制、恶意扩展名、越权、重复提交、部分错误策略
- **完成定义**：仓库只含匿名小型 fixture，错误输出不泄露整行敏感内容

### #11 实现用户、角色、组织和数据范围基础

- **负责人/审查者**：A / B
- **关联需求**：FR-APP-001、FR-QRY-001、AC-011、安全最小权限
- **依赖**：#2、#3
- **允许修改**：身份/权限模块、公共身份契约、迁移/测试、M1 内部身份适配器
- **禁止修改**：正式 SSO、审批业务、前端登录
- **契约影响**：`CurrentUser`、`UserDataScope`、`GET /api/v1/me`；Service 权限依赖
- **数据库影响**：用户、角色、用户角色、组织、数据范围五表
- **验收**：支持组织/楼宇层级和有效期角色；匿名关键请求拒绝；内部适配器可替换且不改变 Service 签名
- **测试**：无身份、角色失效、跨楼宇、全局范围、组织树边界、重复授权、迁移
- **完成定义**：数据范围过滤有可复用测试，禁止仅在前端隐藏越权数据

### #12 实现操作审计基础服务

- **负责人/审查者**：A / B
- **关联需求**：BR-010、FR-RPT-002、AC-012、安全审计要求
- **依赖**：#2、#3、#11
- **允许修改**：`app/modules/audit/`、任务/幂等基础表、公共 AuditContext、迁移/测试
- **禁止修改**：将审计作为公开写 API、记录密钥/无关个人信息、业务主数据
- **契约影响**：AuditService `record`；只读审计查询接口；幂等记录内部服务
- **数据库影响**：`au_operation_log`、`api_idempotency_record`、`task_system_task`
- **验收**：操作日志只追加；前后值脱敏；按对象/操作人/请求查询；同幂等键同载荷返回原结果、不同载荷冲突
- **测试**：不可更新/删除、脱敏、权限查询、请求关联、幂等重放/冲突/过期、任务租约、事务回滚和迁移
- **完成定义**：#7/#9/#10 可通过公开 Service 接入，不直接操作审计表

## 阶段出口

### M1 完成

- 应用可启动，`/health` 可访问；
- 配置、请求 ID、日志、错误响应可验证；
- 空 MySQL 可迁移到 head，且只有一个 head；
- Ruff、格式检查、Pytest 和 CI 通过。

### M2 完成

- 产品、供应商、名单和历史记录可在数据范围内分页维护/查询；
- 白名单有效期与版本正确，黑名单独立复核且优先阻断；
- Excel 导入先校验后确认，重复提交幂等；
- 权限和审计覆盖所有 M2 写操作；
- 所有迁移可从空库执行，接口和数据库文档与实现一致。
